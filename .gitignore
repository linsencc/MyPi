# Dependencies & tooling
node_modules/
.pnp.*
# Lockfile generated per machine / npm version; install from package.json locally
web/package-lock.json

# Build outputs
dist/
build/
out/
*.tsbuildinfo

# Python
.venv/
venv/
__pycache__/
*.py[cod]
*.egg-info/

# Local env & secrets
.env
.env.*
!.env.example

# OS / editor
.DS_Store
Thumbs.db
*.local

# Logs & caches
*.log
.npm/
.cache/

# e-Paper is a separate clone; parent repo tracks sources only
e-Paper/.git/

# Renamed to server/; ignore stray empty pi-server/ if it reappears locally
pi-server/
