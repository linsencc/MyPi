---
description: MyPi 开发→树莓派部署。每次修改 server/web/deploy 等可部署内容后，须在同一次任务内完成 Pi 部署，勿只改本地。
alwaysApply: true
---

# MyPi 部署流程

## 硬性约定：每次修改后要部署

只要本次改动**会影响树莓派上运行的服务或前端**（典型：`server/`、`web/` 需更新 `dist/`、`deploy/` 等），代理在**同一轮对话 / 同一任务收尾前**必须**自行执行**构建、`scp` 与 `systemctl restart`（按下文步骤），并在回复中**简要说明已部署**（或写明无法 SSH 等阻塞原因）。

用户未特别声明「仅本地、不同步 Pi」时，**默认每次改完就部署**。

## 架构

```
开发机 (Windows)            树莓派 (linsen@192.168.1.118)
─────────────────           ─────────────────────────────
MyPi/server/  ──scp──►     ~/workspace/MyPi/server/
MyPi/web/src/ ──build──►   ~/workspace/MyPi/web/dist/
MyPi/deploy/  ──scp──►     ~/workspace/MyPi/deploy/
```

- **SSH 已免密配置**，可直接 `ssh linsen@192.168.1.118`
- Pi 上**没有 git 仓库**，代码通过 scp 推送
- Pi 上**没有前端源码**，只有 `web/dist/` 构建产物
- 后端由 systemd 管理：`mypi.service`（gunicorn gthread）

## 部署步骤

### 1. 推送后端文件（逐个 scp）

```powershell
scp "server\<file>" linsen@192.168.1.118:~/workspace/MyPi/server/<file>
```

新文件需确认目标目录存在：`ssh linsen@192.168.1.118 "mkdir -p ~/workspace/MyPi/server/<dir>"`

### 2. 推送前端（本地构建后推 dist）

```powershell
# 在 web/ 下构建
npm run build
# 推送并原子替换
scp -r "web\dist" linsen@192.168.1.118:~/workspace/MyPi/web/dist_new
ssh linsen@192.168.1.118 "cd ~/workspace/MyPi/web && rm -rf dist_old && mv dist dist_old && mv dist_new dist"
```

### 3. 重启服务

```powershell
ssh linsen@192.168.1.118 "sudo systemctl restart mypi.service"
```

### 4. 验证

```powershell
# 服务状态
ssh linsen@192.168.1.118 "sudo systemctl status mypi.service --no-pager -l"
# 前端可访问
ssh linsen@192.168.1.118 "curl -s -o /dev/null -w '%{http_code}' http://localhost:5050/"
# API 正常
ssh linsen@192.168.1.118 "curl -s http://localhost:5050/api/v1/config | python3 -m json.tool"
# 最近日志
ssh linsen@192.168.1.118 "sudo journalctl -u mypi --no-pager -n 20"
```

## 注意事项

- `server/data/config.json` 是运行时状态文件，推送会**覆盖**用户在 Web UI 的配置，谨慎操作
- 更新 `deploy/mypi.service` 后需额外执行 `sudo cp ... /etc/systemd/system/ && sudo systemctl daemon-reload`
- Pi 是 Raspberry Pi Zero 2 W，资源有限；e-ink 刷新约 27 秒属正常
- Pi 上的 Python venv 在 `~/workspace/MyPi/.venv/`，新增 pip 依赖需 SSH 进去安装

## Agent / 协作约定（与上文「每次修改后要部署」一致）

凡修改**会在 Pi 上运行的内容**，须在同一次任务内完成部署，**禁止**在仅本地改完、未同步 Pi 的情况下结束任务。

**例外**（才可省略当次部署）：用户明确说本次只改本地、不同步 Pi；或当前环境无法 SSH / `scp` 到 `192.168.1.118`（须在回复中说明原因与待补步骤）。

**后端单文件示例**（替换为实际相对路径）：

```powershell
scp "server\renderers\templates\misc_gallery.py" linsen@192.168.1.118:~/workspace/MyPi/server/renderers/templates/misc_gallery.py
ssh linsen@192.168.1.118 "sudo systemctl restart mypi.service"
```
